<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MyMauiApp.StudentDashboardPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="White">

    <Grid>
        <!-- Фон -->
        <Image Source="bg_waves.png"
               Aspect="AspectFill" />

        <Grid Padding="20,40,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Шапка -->
            <VerticalStackLayout Grid.Row="0"
                                 Spacing="8">
                <Label Text="👨‍🎓 Panel ucznia"
                       FontSize="20"
                       FontAttributes="Bold"
                       TextColor="Black"
                       HorizontalOptions="Center"
                       BackgroundColor="White"
                       Padding="12,6" />

                <Frame BackgroundColor="White"
                       CornerRadius="24"
                       Padding="16,8"
                       HasShadow="False">
                    <Label x:Name="StudentNameLabel"
                           Text="Imię ucznia"
                           FontSize="14"
                           TextColor="Black" />
                </Frame>
            </VerticalStackLayout>

            <!-- Список курсов -->
            <CollectionView Grid.Row="1"
                            Margin="0,16,0,0"
                            ItemsSource="{Binding AllCourses}"
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="12"
                               CornerRadius="20"
                               BackgroundColor="White"
                               BorderColor="Black"
                               HasShadow="False"
                               Margin="0,0,0,12">

                            <VerticalStackLayout Spacing="8">
                                <HorizontalStackLayout Spacing="10">
                                    <!-- Иконка курса -->
                                    <Frame WidthRequest="40"
                                           HeightRequest="40"
                                           CornerRadius="20"
                                           Padding="4"
                                           BackgroundColor="#FFEFEF"
                                           HasShadow="False"
                                           VerticalOptions="Center">
                                        <Image Source="{Binding Course.Icon}"
                                               Aspect="AspectFit" />
                                    </Frame>

                                    <!-- Название + язык -->
                                    <VerticalStackLayout>
                                        <Label Text="{Binding Course.Title}"
                                               FontSize="15"
                                               FontAttributes="Bold" />
                                        <Label Text="{Binding Course.Language}"
                                               FontSize="12"
                                               TextColor="#555" />
                                    </VerticalStackLayout>
                                </HorizontalStackLayout>

                                <!-- Статус -->
                                <Label Text="{Binding StatusText}"
                                       FontSize="12"
                                       TextColor="#666"
                                       Margin="0,4,0,0" />

                                <!-- Кнопка действия: либо "Zapisz się", либо "Otwórz" -->
                                <Button Text="{Binding ActionText}"
                                        FontSize="13"
                                        CornerRadius="20"
                                        HeightRequest="40"
                                        BackgroundColor="#002060"
                                        TextColor="White"
                                        CommandParameter="{Binding .}"
                                        Clicked="OnCourseActionClicked" />
                            </VerticalStackLayout>

                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </Grid>
    </Grid>
</ContentPage>