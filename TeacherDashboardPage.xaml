<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MyMauiApp.TeacherDashboardPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:shell="http://schemas.microsoft.com/dotnet/2021/maui"
    shell:Shell.NavBarIsVisible="False"
    BackgroundColor="White">

    <Grid>

        <!-- Фон с волнами -->
        <Image
            Source="bg_waves.png"
            Aspect="AspectFill" />

        <!-- Основная сетка поверх фона -->
        <Grid Padding="20,0,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />   <!-- шапка -->
                <RowDefinition Height="Auto" />   <!-- кнопка Dodaj kurs -->
                <RowDefinition Height="*" />      <!-- список курсов -->
            </Grid.RowDefinitions>

            <!-- ШАПКА ПАНЕЛИ УЧИТЕЛЯ (чёрный блок прямо от верха) -->
            <Grid Grid.Row="0" Margin="-20,0,-20,12">
                <Grid.Background>
                    <SolidColorBrush Color="Black" />
                </Grid.Background>

                <Grid Padding="20,40,20,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />  <!-- бургер + таблетка + аватар -->
                        <RowDefinition Height="Auto" />  <!-- аватар + имя -->
                        <RowDefinition Height="Auto" />  <!-- Zarządzasz 10 kursami -->
                    </Grid.RowDefinitions>

                    <!-- 1. Верхняя строка -->
                    <Grid Grid.Row="0" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Бургер-меню -->
                        <Label
                            Text="☰"
                            FontSize="24"
                            TextColor="White"
                            VerticalOptions="Center" />

                        <!-- Таблетка "Panel nauczyciela" -->
                        <Frame
                            Grid.Column="1"
                            Padding="14,6"
                            BackgroundColor="White"
                            CornerRadius="24"
                            HasShadow="False"
                            HorizontalOptions="Center">
                            <HorizontalStackLayout Spacing="6">
                                <Label
                                    Text="👨‍🏫"
                                    FontSize="16"
                                    VerticalOptions="Center" />
                                <Label
                                    Text="Panel nauczyciela"
                                    FontSize="14"
                                    FontAttributes="Bold"
                                    TextColor="Black"
                                    VerticalOptions="Center" />
                            </HorizontalStackLayout>
                        </Frame>

                        <!-- Иконка пользователя -->
                        <Frame
                            Grid.Column="2"
                            HeightRequest="36"
                            WidthRequest="36"
                            CornerRadius="18"
                            BackgroundColor="White"
                            Padding="0"
                            VerticalOptions="Center">
                            <Label
                                Text="👤"
                                FontSize="20"
                                HorizontalOptions="Center"
                                VerticalOptions="Center" />
                        </Frame>
                    </Grid>

                    <!-- 2. Аватар + имя -->
                    <Grid
                        Grid.Row="1"
                        ColumnSpacing="12"
                        Margin="0,18,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Круглый аватар -->
                        <Frame
                            HeightRequest="60"
                            WidthRequest="60"
                            CornerRadius="30"
                            BackgroundColor="White"
                            Padding="0"
                            VerticalOptions="Center">
                            <Label
                                Text="👩‍💻"
                                FontSize="30"
                                HorizontalOptions="Center"
                                VerticalOptions="Center" />
                        </Frame>

                        <!-- Таблетка с именем -->
                        <Frame
                            Grid.Column="1"
                            BackgroundColor="White"
                            CornerRadius="24"
                            HasShadow="False"
                            Padding="16,8"
                            VerticalOptions="Center">
                            <Label
                                Text="Imię i nazwisko nauczyciela"
                                FontSize="14"
                                TextColor="Black" />
                        </Frame>
                    </Grid>

                    <!-- 3. "Zarządzasz 10 kursami" + Synergia -->
                    <Frame
                        Grid.Row="2"
                        BackgroundColor="White"
                        CornerRadius="24"
                        HasShadow="False"
                        Padding="16,8"
                        Margin="0,18,0,0">
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Label
                                Text="Zarządzasz 10 kursami"
                                FontSize="13"
                                TextColor="Black"
                                VerticalOptions="Center" />

                            <Frame
                                Grid.Column="1"
                                HeightRequest="28"
                                WidthRequest="28"
                                CornerRadius="14"
                                BackgroundColor="Black"
                                Padding="2"
                                VerticalOptions="Center">
                                <Image
                                    Source="synergia_icon.png"
                                    Aspect="AspectFit" />
                            </Frame>
                        </Grid>
                    </Frame>
                </Grid>
            </Grid>

            <!-- Кнопка: добавить курс -->
            <Button
                Grid.Row="1"
                Text="DODAJ KURS"
                BackgroundColor="#002060"
                TextColor="White"
                CornerRadius="24"
                HeightRequest="48"
                Margin="0,24,0,12"
                Clicked="OnAddCourseClicked" />

            <!-- Список курсов -->
            <ScrollView Grid.Row="2">
                <VerticalStackLayout Spacing="16">

                    <!-- Активные курсы -->
                    <Label
                        Text="Aktywne kursy"
                        FontSize="16"
                        FontAttributes="Bold"
                        TextColor="Black" />

                    <!-- Swift -->
                    <Frame
                        Padding="12"
                        CornerRadius="20"
                        BackgroundColor="White"
                        BorderColor="Black"
                        HasShadow="False">
                        <VerticalStackLayout Spacing="8">
                            <HorizontalStackLayout Spacing="10">
                                <Image
                                    Source="swift_icon.png"
                                    HeightRequest="32"
                                    WidthRequest="32" />
                                <VerticalStackLayout>
                                    <Label
                                        Text="Swift Development"
                                        FontSize="15"
                                        FontAttributes="Bold" />
                                    <Label
                                        Text="42 wykłady | 37 uczniów"
                                        FontSize="12"
                                        TextColor="#555" />
                                </VerticalStackLayout>
                            </HorizontalStackLayout>

                            <HorizontalStackLayout
                                Spacing="10"
                                Margin="0,6,0,0">
                                <Button
                                    Text="Dodaj materiał"
                                    FontSize="12"
                                    CornerRadius="20"
                                    BackgroundColor="#002060"
                                    TextColor="White"
                                    HeightRequest="36"
                                    CommandParameter="Swift Development"
                                    Clicked="OnAddMaterialClicked" />
                                <Button
                                    Text="Postępy uczniów"
                                    FontSize="12"
                                    CornerRadius="20"
                                    BackgroundColor="White"
                                    TextColor="#002060"
                                    BorderColor="#002060"
                                    BorderWidth="1"
                                    HeightRequest="36"
                                    CommandParameter="Swift Development"
                                    Clicked="OnStudentsProgressClicked" />
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Python -->
                    <Frame
                        Padding="12"
                        CornerRadius="20"
                        BackgroundColor="White"
                        BorderColor="Black"
                        HasShadow="False">
                        <VerticalStackLayout Spacing="8">
                            <HorizontalStackLayout Spacing="10">
                                <Image
                                    Source="python_icon.png"
                                    HeightRequest="32"
                                    WidthRequest="32" />
                                <VerticalStackLayout>
                                    <Label
                                        Text="Python Development"
                                        FontSize="15"
                                        FontAttributes="Bold" />
                                    <Label
                                        Text="34 wykłady | 28 uczniów"
                                        FontSize="12"
                                        TextColor="#555" />
                                </VerticalStackLayout>
                            </HorizontalStackLayout>

                            <HorizontalStackLayout
                                Spacing="10"
                                Margin="0,6,0,0">
                                <Button
                                    Text="Dodaj materiał"
                                    FontSize="12"
                                    CornerRadius="20"
                                    BackgroundColor="#002060"
                                    TextColor="White"
                                    HeightRequest="36"
                                    CommandParameter="Python Development"
                                    Clicked="OnAddMaterialClicked" />
                                <Button
                                    Text="Postępy uczniów"
                                    FontSize="12"
                                    CornerRadius="20"
                                    BackgroundColor="White"
                                    TextColor="#002060"
                                    BorderColor="#002060"
                                    BorderWidth="1"
                                    HeightRequest="36"
                                    CommandParameter="Python Development"
                                    Clicked="OnStudentsProgressClicked" />
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Архивные курсы -->
                    <Label
                        Text="Zarchiwizowane kursy"
                        FontSize="14"
                        FontAttributes="Bold"
                        TextColor="#555"
                        Margin="0,12,0,0" />

                    <!-- Java (архив) -->
                    <Frame
                        Padding="12"
                        CornerRadius="20"
                        BackgroundColor="#FFE5E5"
                        BorderColor="Red"
                        HasShadow="False">
                        <VerticalStackLayout Spacing="8">
                            <HorizontalStackLayout Spacing="10">
                                <Image
                                    Source="java_icon.png"
                                    HeightRequest="32"
                                    WidthRequest="32" />
                                <VerticalStackLayout Spacing="4">
                                    <Label
                                        Text="Java Development"
                                        FontSize="15"
                                        FontAttributes="Bold" />
                                    <Label
                                        Text="28 wykładów | 20 godzin"
                                        FontSize="12"
                                        TextColor="#555" />
                                </VerticalStackLayout>
                            </HorizontalStackLayout>
                        </VerticalStackLayout>
                    </Frame>

                </VerticalStackLayout>
            </ScrollView>

        </Grid>
    </Grid>
</ContentPage>