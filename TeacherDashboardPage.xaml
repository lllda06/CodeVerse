<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MyMauiApp.TeacherDashboardPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:shell="http://schemas.microsoft.com/dotnet/2021/maui"
    shell:Shell.NavBarIsVisible="False"
    BackgroundColor="White">

    <Grid>
        <!-- Фоновая картинка -->
        <Image Source="bg_waves.png"
               Aspect="AspectFill" />

        <!-- Основная сетка -->
        <Grid Padding="20,0,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Шапка (чёрный блок) -->
            <Grid Grid.Row="0"
                  Margin="-20,0,-20,12">
                <Grid.Background>
                    <SolidColorBrush Color="Black" />
                </Grid.Background>

                <Grid Padding="20,40,20,24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- 1. Верхняя строка -->
                    <Grid Grid.Row="0"
                          ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Бургер -->
                        <Label Text="☰"
                               FontSize="24"
                               TextColor="White"
                               VerticalOptions="Center" />

                        <!-- Таблетка "Panel nauczyciela" -->
                        <Frame Grid.Column="1"
                               Padding="14,6"
                               BackgroundColor="White"
                               CornerRadius="24"
                               HasShadow="False"
                               HorizontalOptions="Center">
                            <HorizontalStackLayout Spacing="6">
                                <Label Text="👨‍🏫"
                                       FontSize="16"
                                       VerticalOptions="Center" />
                                <Label Text="Panel nauczyciela"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="Black"
                                       VerticalOptions="Center" />
                            </HorizontalStackLayout>
                        </Frame>

                        <!-- Иконка профиля -->
                        <Frame Grid.Column="2"
                               HeightRequest="36"
                               WidthRequest="36"
                               CornerRadius="18"
                               BackgroundColor="White"
                               Padding="0"
                               VerticalOptions="Center">
                            <Label Text="👤"
                                   FontSize="20"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />
                        </Frame>
                    </Grid>

                    <!-- 2. Аватар + имя -->
                    <Grid Grid.Row="1"
                          ColumnSpacing="12"
                          Margin="0,18,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Аватар -->
                        <Frame HeightRequest="60"
                               WidthRequest="60"
                               CornerRadius="30"
                               BackgroundColor="White"
                               Padding="0"
                               VerticalOptions="Center">
                            <Label Text="👩‍💻"
                                   FontSize="30"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />
                        </Frame>

                        <!-- Имя учителя -->
                        <Frame Grid.Column="1"
                               BackgroundColor="White"
                               CornerRadius="24"
                               HasShadow="False"
                               Padding="16,8"
                               VerticalOptions="Center">
                            <Label x:Name="TeacherNameLabel"
                                   Text="Imię i nazwisko nauczyciela"
                                   FontSize="14"
                                   TextColor="Black" />
                        </Frame>
                    </Grid>

                    <!-- 3. Кол-во курсов -->
                    <Frame Grid.Row="2"
                           BackgroundColor="White"
                           CornerRadius="24"
                           HasShadow="False"
                           Padding="16,8"
                           Margin="0,18,0,0">
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Label x:Name="CoursesCountLabel"
                                   Text="Zarządzasz 0 kursami"
                                   FontSize="13"
                                   TextColor="Black"
                                   VerticalOptions="Center" />

                            <Frame Grid.Column="1"
                                   HeightRequest="28"
                                   WidthRequest="28"
                                   CornerRadius="14"
                                   BackgroundColor="Black"
                                   Padding="2"
                                   VerticalOptions="Center">
                                <Image Source="synergia_icon.png"
                                       Aspect="AspectFit" />
                            </Frame>
                        </Grid>
                    </Frame>
                </Grid>
            </Grid>

            <!-- Кнопка "Dodaj kurs" -->
            <Button Grid.Row="1"
                    Text="DODAJ KURS"
                    BackgroundColor="#002060"
                    TextColor="White"
                    CornerRadius="24"
                    HeightRequest="48"
                    Margin="0,24,0,12"
                    Clicked="OnAddCourseClicked" />

            <!-- Список курсов учителя -->
            <CollectionView Grid.Row="2"
                            ItemsSource="{Binding TeacherCourses}"
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="12"
                               CornerRadius="20"
                               BackgroundColor="White"
                               BorderColor="Black"
                               HasShadow="False"
                               Margin="0,0,0,12">

                            <VerticalStackLayout Spacing="8">
                                <HorizontalStackLayout Spacing="10">
                                    <!-- Иконка курса (динамическая) -->
                                    <Frame WidthRequest="40"
                                           HeightRequest="40"
                                           CornerRadius="20"
                                           Padding="4"
                                           BackgroundColor="#FFEFEF"
                                           HasShadow="False"
                                           VerticalOptions="Center">
                                        <Image Source="{Binding Icon}"
                                               Aspect="AspectFit" />
                                    </Frame>

                                    <!-- Название + язык -->
                                    <VerticalStackLayout>
                                        <Label Text="{Binding Title}"
                                               FontSize="15"
                                               FontAttributes="Bold" />
                                        <Label Text="{Binding Language}"
                                               FontSize="12"
                                               TextColor="#555" />
                                    </VerticalStackLayout>
                                </HorizontalStackLayout>

                                <!-- Кнопки действий -->
                                <HorizontalStackLayout Spacing="10"
                                                       Margin="0,6,0,0">
                                    <Button Text="Dodaj materiał"
                                            FontSize="12"
                                            CornerRadius="20"
                                            BackgroundColor="#002060"
                                            TextColor="White"
                                            HeightRequest="36"
                                            CommandParameter="{Binding .}"
                                            Clicked="OnAddMaterialClicked" />
                                    <Button Text="Postępy uczniów"
                                            FontSize="12"
                                            CornerRadius="20"
                                            BackgroundColor="White"
                                            TextColor="#002060"
                                            BorderColor="#002060"
                                            BorderWidth="1"
                                            HeightRequest="36"
                                            CommandParameter="{Binding .}"
                                            Clicked="OnStudentsProgressClicked" />
                                </HorizontalStackLayout>

                                <!-- Кнопка для удаления курса -->
                                <Button Text="Usuń kurs"
                                        FontSize="12"
                                        CornerRadius="20"
                                        BackgroundColor="Red"
                                        TextColor="White"
                                        HeightRequest="36"
                                        CommandParameter="{Binding .}"
                                        Clicked="OnDeleteCourseClicked" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </Grid>
    </Grid>
</ContentPage>